% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/GET_subset_discriminant.R
\name{get_subset_discriminant}
\alias{get_subset_discriminant}
\title{Filter feature table by discriminant score}
\usage{
get_subset_discriminant(
  tibble_rbims,
  analysis,
  metadata,
  group_col,
  score_min = 1,
  min_presence = 2,
  feature_col = NULL
)
}
\arguments{
\item{tibble_rbims}{A tibble returned by \code{consensus_rank()} (or \code{rbims_consensus()} ...),
containing at least the columns \code{feature} and \code{score}.}

\item{analysis}{Character: "KEGG", "Pfam", "INTERPRO", "dbCAN", or "MEROPS"}

\item{metadata}{MAG-level metadata (rownames = MAG identifiers)}

\item{group_col}{Metadata column to discriminate (e.g. "Depth", "Class", "Phylum")}

\item{score_min}{Minimum consensus score (default = 2)}

\item{min_presence}{Minimum number of MAGs in which a feature must appear}

\item{feature_col}{Column name containing feature IDs}
}
\value{
Filtered tibble (same format as input) with the full
discriminant object attached as attribute "rbims_disc".
}
\description{
Wrapper around \code{get_discriminant_features()} that returns
only features with consensus score ≥ threshold.
}
